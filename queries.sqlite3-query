-- database: ./Database.db

-- Everything
SELECT * FROM Products;

-- ID, manufacturer, image url
SELECT Products.product_id, Products.product_name, Products.image_url, Products.price 
FROM Products
JOIN manufacturers ON manufacturers.manufacturer_id = Products.manufacturer_id;

-- All violins based on product type
SELECT * FROM Products 
WHERE Products.product_type = 'Violin';

